{
  "version": 3,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "^/uploads/(.*)$", "dest": "/uploads/$1" },
    { "src": "^/(.*)$", "dest": "/index.html" }
  ]
}
